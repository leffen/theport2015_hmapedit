process.env.NODE_ENV = 'test';

var assert = require("assert");

var map_enhancer = require('../source/db.js');

describe('DB', function() {

  describe('maps', function () {
    it('It should be created once', function () {
      console.log("Started");
    });
  });


});